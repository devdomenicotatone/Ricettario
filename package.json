{
  "name": "ricettario-ultra-pro",
  "private": true,
  "version": "1.0.0",
  "description": "Il Ricettario — Ricette artigianali ottimizzate per Famag Grilletta, Fimar PF25E e Philips Serie 7000",
  "author": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:recipes": "node scripts/build-recipes.js",
    "build:cottura": "node scripts/build-cottura.js",
    "generate-og": "node scripts/generate-og.js",
    "verifica": "node scripts/verifica-build.js",
    "check": "npm run build:recipes && npm run build:cottura && vite build && npm run generate-og && npm run verifica",
    "deploy": "npm run check && node scripts/deploy-ghpages.js"
  },
  "devDependencies": {
    "chart.js": "^4.5.1",
    "gh-pages": "^6.3.0",
    "vite": "^6.1.0"
  }
}
